// Fill this in with your own free Firebase project to turn on live cross-device
// sync. Until you do, the app works exactly as before — everything just stays
// on the one device that's using it. See README.md, "Enable live sync", for
// the full step-by-step.
//
// After creating a Realtime Database in your Firebase project, replace the
// object below with the config block Firebase gives you (Project settings ->
// General -> Your apps -> SDK setup and configuration).

window.FIREBASE_CONFIG = {
  apiKey: 'REPLACE_ME',
  authDomain: 'REPLACE_ME.firebaseapp.com',
  databaseURL: 'https://REPLACE_ME-default-rtdb.firebaseio.com',
  projectId: 'REPLACE_ME',
  storageBucket: 'REPLACE_ME.appspot.com',
  messagingSenderId: 'REPLACE_ME',
  appId: 'REPLACE_ME',
};
